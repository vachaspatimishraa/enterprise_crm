/// Identifies the 8 CRM business modules.
///
/// This domain enum is completely Flutter-independent. UI metadata such as
/// icons, display titles, and subtitles are managed in the presentation layer.
enum CrmModule {
  leadManagement,
  calling,
  inventory,
  dispatch,
  purchase,
  hrPayroll,
  approvalsNotifications,
  vendorManagement,
}
